export interface ICountryFormModel {
    id: string;
    name: string;
    code: string;
}

export interface ICountryListingModel {
    id: string;
    name: string;
    code: string;
    isActive: boolean;
}